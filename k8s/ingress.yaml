apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: redis-graph-agent
  namespace: your-namespace
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    nginx.ingress.kubernetes.io/cors-allow-headers: Content-Type
    nginx.ingress.kubernetes.io/cors-allow-methods: POST, GET, OPTIONS
    nginx.ingress.kubernetes.io/enable-cors: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: redis-agent-dev.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: redis-graph-agent
                port:
                  number: 8080
  tls:
    - hosts:
        - redis-agent-dev.example.com
      secretName: redis-agent-dev.example.com-tls
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: redis-mcp-server
  namespace: your-namespace
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    nginx.ingress.kubernetes.io/cors-allow-headers: Content-Type
    nginx.ingress.kubernetes.io/cors-allow-methods: GET, OPTIONS
    nginx.ingress.kubernetes.io/enable-cors: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: redis-mcp-dev.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: redis-mcp-server
                port:
                  number: 8000
  tls:
    - hosts:
        - redis-mcp-dev.example.com
      secretName: redis-mcp-dev.example.com-tls
